# TRƎNZA — Pre-launch

Cloudflare Workers + Static Assets + D1 hazır sürüm.

- Static site: `public/`
- Worker/API: `worker/index.js`
- API handlers: `functions/api/`
- D1 schema: `public/schema.sql` (excluded from uploaded assets)
- Wrangler: `wrangler.jsonc`

Production domain: `https://trenza.com.tr/`

Ödeme ve gerçek satış kapalıdır.
